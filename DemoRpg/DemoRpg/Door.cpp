#include "Door.h"
Door::Door(char column, char row, bool boss)
{
    tilePositionColumn = column;
	tilePositionRow = row;
	bossDoor = boss;
	if(!bossDoor)
	{
		assetSheetPosition =
	}
	else
	{
		assetSheetPosition =
	}
}